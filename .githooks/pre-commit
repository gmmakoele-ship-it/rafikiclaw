#!/usr/bin/env sh
set -eu

if [ "${RAFIKICLAW_SKIP_SECRET_SCAN:-}" = "1" ]; then
  echo "pre-commit: skipping secret scan (RAFIKICLAW_SKIP_SECRET_SCAN=1)"
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
"$repo_root/scripts/scan-secrets.sh" --staged
